---
client:
  hosts: <%= p("curator.elasticsearch.hosts") %>
  port: <%= p("curator.elasticsearch.port") %>
  url_prefix:
  use_ssl: False
  certificate:
  client_cert:
  client_key:
  ssl_no_validate: False
  http_auth:
  timeout: 30
  master_only: False

logging:
  loglevel: <%= p('curator.loglevel') %>
  logfile:
  logformat: <%= p('curator.logformat') %>
  blacklist: ['elasticsearch', 'urllib3']
